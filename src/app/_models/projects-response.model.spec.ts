import { ProjectsResponse } from './projects-response.model';

describe('ProjectsResponse', () => {
  it('should create an instance', () => {
    expect(new ProjectsResponse()).toBeTruthy();
  });
});
